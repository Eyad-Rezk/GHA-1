name: 1st-workflow

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - main
  #   paths:
  #     - "dir-1/**"

jobs:
  manual-job:
    runs-on: ubuntu-latest
    steps:
      - name: manual-step-1
        run: echo "Hello from manual workflow!"
